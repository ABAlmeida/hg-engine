# Heartless Gold Repository Context

This repository is the source of truth for Pokémon Heartless Gold. The latest
recorded integrated hg-engine upstream is commit
`dacca858e9f917607a20090a76f26df8e53849ed`; it is not the current Heartless
Gold `main` revision.

As checked on 2026-09-11, `origin` is
`https://github.com/ABAlmeida/hg-engine.git` and the configured `upstream` is
hg-engine's canonical `https://github.com/BluRosie/hg-engine.git`. Inspect
current Git state before relying on this recorded context.

`tools/source/nitrogfx` is a required Git submodule. Build/setup work should
follow `.agents/skills/hg-engine-build-and-recovery/SKILL.md`.
